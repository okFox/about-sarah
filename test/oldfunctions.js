/* creating a function that can be used in the test. don't forget to import i test.js */


/* TEST THIS FUNCTION */

export const isYes = (someString) => {
    if(someString === 'yes' || someString === 'y') {
        return true;
    } else {
        return false;

    }
};